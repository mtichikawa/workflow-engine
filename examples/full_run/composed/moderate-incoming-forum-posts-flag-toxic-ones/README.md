# moderate-incoming-forum-posts-flag-toxic-ones

**Use case:** moderate incoming forum posts: flag toxic ones and route the rest to the right subforum

**Input:** You're all idiots if you use tabs over spaces, this whole thread is garbage.

Composed by the engine, then run end-to-end. `run.json` = the full log; `output/` = what it staged.
