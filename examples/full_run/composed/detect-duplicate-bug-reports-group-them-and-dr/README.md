# detect-duplicate-bug-reports-group-them-and-dr

**Use case:** detect duplicate bug reports, group them, and draft a merge note

**Input:** App crashes on startup right after the latest update on iOS 17.

**Drafted specialists (provisional):** draft_merge_note, find_duplicates

Composed by the engine, then run end-to-end. `run.json` = the full log; `output/` = what it staged.
