# read-github-issues-label-and-prioritize-them-a

**Use case:** read GitHub issues, label and prioritize them, and draft a first response

**Input:** Memory leak in the worker pool under high load; RSS grows unbounded over ~6 hours.

Composed by the engine, then run end-to-end. `run.json` = the full log; `output/` = what it staged.
