# generate-a-product-image-from-a-text-descripti

**Use case:** generate a product image from a text description and stage it

**Input:** A minimalist matte-white ceramic coffee mug on a wooden table in soft morning light.

**Drafted specialists (provisional):** generate_image, stage_image

Composed by the engine, then run end-to-end. `run.json` = the full log; `output/` = what it staged.
