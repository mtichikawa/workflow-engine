# review-expense-reports-and-flag-ones-that-viol

**Use case:** review expense reports and flag ones that violate policy

**Input:** $820 client dinner for two, entertainment category, no itemized receipt attached.

Composed by the engine, then run end-to-end. `run.json` = the full log; `output/` = what it staged.
