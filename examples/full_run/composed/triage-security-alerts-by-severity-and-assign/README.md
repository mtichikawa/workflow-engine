# triage-security-alerts-by-severity-and-assign

**Use case:** triage security alerts by severity and assign them to the right on-call

**Input:** Alert: unusual outbound traffic from prod-db-03 to an unknown IP — 2GB in 10 minutes.

**Drafted specialists (provisional):** assign_oncall

Composed by the engine, then run end-to-end. `run.json` = the full log; `output/` = what it staged.
