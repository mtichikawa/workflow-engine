# review-pull-requests-summarize-the-diff-flag-r

**Use case:** review pull requests: summarize the diff, flag risky changes, and suggest a reviewer

**Input:** PR: refactor the auth middleware; touches session handling and token refresh logic.

**Drafted specialists (provisional):** suggest_reviewer

Composed by the engine, then run end-to-end. `run.json` = the full log; `output/` = what it staged.
